# 🔍 Unique Lines

<div align="center">

![Python](https://img.shields.io/badge/Python-3.7+-blue.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

**Утилита для удаления дубликатов строк из текстовых файлов**

[Описание](#-описание) • [Возможности](#-возможности) • [Установка](#-установка) • [Использование](#-использование)

</div>

---

## 📋 Описание

Unique Lines — это простой и эффективный скрипт для удаления дубликатов строк из текстовых файлов. Скрипт использует MD5 хеширование для оптимизации использования памяти при работе с большими файлами, что позволяет обрабатывать файлы любого размера без загрузки всего содержимого в оперативную память.

## ✨ Возможности

- 🚀 **Эффективная обработка** — использование MD5 хеширования для экономии RAM при работе с большими файлами
- 📊 **Статистика обработки** — показывает количество уникальных строк и удаленных дубликатов
- ⚙️ **Гибкая настройка** — поддержка аргументов командной строки в разных форматах
- 🔒 **Обработка ошибок** — корректная обработка ошибок доступа и других исключений
- 💾 **Сохранение оригинального порядка** — уникальные строки сохраняются в том же порядке, в котором они встречались в исходном файле
- ⚠️ **Защита от перезаписи** — предупреждение перед перезаписью существующего файла

## 🚀 Установка

### Требования

- Python 3.7 или выше
- Стандартная библиотека Python (внешние зависимости не требуются)

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/CHUKEPC/unique-lines.git
cd unique-lines
```

2. **Готово!** Дополнительные зависимости не требуются.

## 📖 Использование

### Базовое использование

**Удалить дубликаты из файла:**
```bash
python main.py input.txt output.txt
```

**Использование с флагами:**
```bash
python main.py -i input.txt -o output.txt
```

**Использование с длинными флагами:**
```bash
python main.py --input large_file.txt --output cleaned_file.txt
```

### Примеры использования

**Пример 1: Обработка большого файла**
```bash
$ python main.py large_input.txt cleaned_output.txt
Обработка файла: large_input.txt

✓ Готово! Очищенные данные сохранены в: cleaned_output.txt
  Уникальных строк: 15234
  Дубликатов удалено: 8766
  Всего обработано: 24000
```

**Пример 2: Использование флагов**
```bash
$ python main.py -i data.txt -o unique_data.txt
Обработка файла: data.txt

✓ Готово! Очищенные данные сохранены в: unique_data.txt
  Уникальных строк: 500
  Дубликатов удалено: 0
  Всего обработано: 500
```

**Пример 3: Перезапись существующего файла**
```bash
$ python main.py input.txt output.txt
Выходной файл 'output.txt' уже существует. Перезаписать? (y/n): y
Обработка файла: input.txt

✓ Готово! Очищенные данные сохранены в: output.txt
  Уникальных строк: 1000
  Дубликатов удалено: 250
  Всего обработано: 1250
```

### Аргументы командной строки

| Аргумент | Короткая форма | Описание |
|----------|---------------|----------|
| `input_file` | - | Путь к входному файлу (позиционный аргумент) |
| `output_file` | - | Путь к выходному файлу (позиционный аргумент) |
| `--input` | `-i` | Путь к входному файлу (альтернативный способ) |
| `--output` | `-o` | Путь к выходному файлу (альтернативный способ) |
| `--help` | `-h` | Показать справку по использованию |

## 🔧 Как это работает

1. **Чтение файла построчно** — скрипт читает входной файл построчно, не загружая весь файл в память
2. **Хеширование строк** — для каждой строки генерируется MD5 хеш (16 байт вместо полной строки)
3. **Проверка уникальности** — хеш проверяется в множестве уже встреченных хешей
4. **Запись уникальных строк** — уникальные строки записываются в выходной файл в порядке их появления
5. **Статистика** — подсчитывается количество уникальных строк и дубликатов

### Преимущества использования хеширования

- **Экономия памяти**: вместо хранения полных строк в памяти хранятся только 16-байтовые хеши
- **Быстрая проверка**: проверка наличия хеша в множестве выполняется за O(1)
- **Масштабируемость**: позволяет обрабатывать файлы с миллионами строк

## 📁 Структура проекта

```
unique_lines/
│
├── main.py        # Основной скрипт удаления дубликатов
├── README.md      # Документация проекта
└── .gitignore     # Игнорируемые файлы для Git
```

## ⚠️ Важные замечания

- **Кодировка**: Скрипт работает с файлами в кодировке UTF-8
- **Порядок строк**: Уникальные строки сохраняются в том же порядке, в котором они встречались в исходном файле
- **Первое вхождение**: При наличии дубликатов сохраняется первое вхождение строки
- **Пустые строки**: Пустые строки также обрабатываются и считаются уникальными, если они встречаются впервые
- **Большие файлы**: Скрипт оптимизирован для работы с большими файлами благодаря использованию хеширования

## 🔍 Примеры работы

### Пример входного файла (input.txt)
```
строка 1
строка 2
строка 1
строка 3
строка 2
строка 4
```

### Результат (output.txt)
```
строка 1
строка 2
строка 3
строка 4
```

### Вывод скрипта
```
Обработка файла: input.txt

✓ Готово! Очищенные данные сохранены в: output.txt
  Уникальных строк: 4
  Дубликатов удалено: 2
  Всего обработано: 6
```

## 🐛 Обработка ошибок

Скрипт корректно обрабатывает следующие ситуации:

- **Файл не найден**: Выводит понятное сообщение об ошибке
- **Нет доступа к файлу**: Информирует о проблемах с правами доступа
- **Существующий выходной файл**: Предупреждает и запрашивает подтверждение перед перезаписью
- **Другие ошибки**: Выводит информацию об ошибке и завершает работу с кодом ошибки

## 🤝 Вклад в проект

Вклад в проект приветствуется! Если у вас есть идеи по улучшению:

1. Fork проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👤 Автор

**CHUKEPC**
- GitHub: [@CHUKEPC](https://github.com/CHUKEPC)
- Telegram: [@ArtyomRoyal](https://t.me/ArtyomRoyal)

---

<div align="center">

⭐ Если проект был полезен, поставьте звезду! ⭐

</div>
